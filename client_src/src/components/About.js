import React from 'react';

const About = () => (
  <div>
    <h1>About</h1>
    <p>This is a project done with ReactJS, Materialize, NodeJS/Express, and the Loopback API.
    Loopback was used to create an API of mock meetups in the San Diego area. ReactJS is using Axios to fetch
    make a request to /api/meetups to return the data to be displayed.</p>
  </div>
)

export default About;
